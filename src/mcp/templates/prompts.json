[
  {
    "name": "security_check_program",
    "description": "Given a program path, check if program passes solana fender checks",
    "arguments": [
      {
        "name": "program_path",
        "description": "Path to anchor program folder to check",
        "required": true
      }
    ]
  },
  {
    "name": "security_check_file",
    "description": "Given a file path, check if file passes solana fender checks",
    "arguments": [
      {
        "name": "file_path",
        "description": "Path to anchor file folder to check",
        "required": true
      }
    ]
  }
]
